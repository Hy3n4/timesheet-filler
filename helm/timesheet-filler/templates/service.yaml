apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-timesheet-filler
  labels:
    app: timesheet-filler
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: timesheet-filler
    release: {{ .Release.Name }}
